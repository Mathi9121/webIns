{% extends '::app.html.twig' %}

{% block body %}
    <h1>Liste des formations</h1>
    <table class="table-striped width-100">
        <thead>
            <tr>
                <th></th>
                <th class="width-50">Intitulé</th>
                <th>Lieu</th>
                <th>Date</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a title="Voir les inscriptions/personnes associées" class="btn btn-small" href="#"><span class="oi" data-glyph="people"></span></a></td>
                <td>{{ entity.intitule }}</td>
                <td>{{ entity.lieu }}</td>
                <td>{{ entity.dateText }}</td>
                <td>
					<div class="group">
						<a title="Voir la fiche" class="btn btn-small" href="{{ path('formation_show', { 'id': entity.id }) }}"><span class="oi" data-glyph="info"></span></a>
						<a title="Configuration" class="btn btn-small" href="{{ path('formation_edit', { 'id': entity.id }) }}"><span class="oi" data-glyph="cog"></span></a>
					</div>
				</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

            <a href="{{ path('formation_new') }}" class="btn">
                Nouvelle formation
            </a>

    {% endblock %}
